# Ansible Role for install local Minio S3 Storage

## Разворачивание:

* inventory.ini - список серверов для применения плейбука
* vars/main.yml - параметры плейбука
 
* Запуск:

`ansible-playbook -i inventory.ini example.yml`